package com.waitlist.domain.entity;

public enum SubscriptionPlan {
    BASIC,      // Basic plan - limited features
    PRO,        // Professional plan - full features
    ENTERPRISE  // Enterprise plan - unlimited features + priority support
}

